package com.exilant.traing.day002.predicate;

import java.util.Random;
import java.util.concurrent.ThreadLocalRandom;
import java.util.stream.Stream;

public class numberRandomFilterExample {

	
	public static void main(String[] args) {
		int randomNumber=ThreadLocalRandom.current().nextInt(100,110);
		//System.out.println(randomNumber);
		Random random= new Random();
		//random.ints(10,100,200).forEach(System.out::println);
		//Sorted order
		//random.ints(10,100,110).sorted().forEach(System.out::println);
		
		
		
		
		
	}
}
